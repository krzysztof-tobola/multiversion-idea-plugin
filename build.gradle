import com.github.tobolak.build.InheritSourcesPlugin

plugins {
    id "org.jetbrains.intellij" version "0.2.18" apply false
}

subprojects {
    apply plugin: "org.jetbrains.intellij"
    apply plugin: "java"
    apply plugin: InheritSourcesPlugin

    ext.inheritSourcesFrom = parent
}
